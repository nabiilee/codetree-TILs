#include <iostream>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int mm, dd, yyyy;
    char d;
    cin >> mm >> d >> dd >> d >> yyyy;

    cout << yyyy << "." << mm << "." << dd;
    return 0;
}